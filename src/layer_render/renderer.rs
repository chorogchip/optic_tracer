
use crate::layer_cmd::options::Options;
use crate::layer_scenedata::processed_scene::ProcessedScene;
use super::output_raw_data::OutputRawData;

pub fn render_scene(scene: ProcessedScene, options: &Options) -> OutputRawData {
    return OutputRawData{ };
}