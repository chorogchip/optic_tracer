
use crate::layer_cmd::options::Options;

pub struct InputScene {
    
}

impl InputScene {
    pub fn new() -> InputScene {
        InputScene {

        }
    }
}

pub fn read_scene(options: &Options) -> InputScene {
    return InputScene::new();
}